"use client"
import { useEffect, useState } from "react"
import { useRouter } from "next/navigation"
import { Command, CommandDialog, CommandEmpty, CommandGroup, CommandInput, CommandItem, CommandList } from "@/components/ui/command"
import { LayoutDashboard, Radio, Smartphone, GraduationCap, Tv, WalletCards, ReceiptText, Settings, Sun, Moon, Monitor } from "lucide-react"
import { useTheme } from "next-themes"
export function CommandPalette(){const [open,setOpen]=useState(false);const router=useRouter();const {setTheme}=useTheme();useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(v=>!v)}};addEventListener("keydown",fn);return()=>removeEventListener("keydown",fn)},[]);const pages=[['Overview','/dashboard',LayoutDashboard],['Buy Data','/dashboard/data',Radio],['Airtime VTU','/dashboard/airtime',Smartphone],['Exam PINs','/dashboard/exam-pins',GraduationCap],['Cable TV & Power','/dashboard/bills',Tv],['Wallet & Funding','/dashboard/wallet',WalletCards],['Transactions','/dashboard/transactions',ReceiptText],['Settings','/dashboard/settings',Settings]] as const;return <CommandDialog open={open} onOpenChange={setOpen} title="TEKSUM Command Palette" description="Navigate TEKSUM services"><Command><CommandInput placeholder="Search TEKSUM services..."/><CommandList><CommandEmpty>No service found.</CommandEmpty><CommandGroup heading="Services">{pages.map(([label,href,Icon])=><CommandItem key={href} onSelect={()=>{setOpen(false);router.push(href)}}><Icon className="mr-2 size-4"/>{label}</CommandItem>)}</CommandGroup><CommandGroup heading="Theme"><CommandItem onSelect={()=>{setOpen(false);setTheme('light')}}><Sun className="mr-2 size-4"/>Light</CommandItem><CommandItem onSelect={()=>{setOpen(false);setTheme('dark')}}><Moon className="mr-2 size-4"/>Dark</CommandItem><CommandItem onSelect={()=>{setOpen(false);setTheme('system')}}><Monitor className="mr-2 size-4"/>System</CommandItem></CommandGroup></CommandList></Command></CommandDialog>}
