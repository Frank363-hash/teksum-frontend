import Link from "next/link"
import { notFound } from "next/navigation"
import { ArrowLeft, ArrowRight } from "lucide-react"
import { educationProviders } from "@/components/teksum/service-discovery"
export default async function Page({params}:{params:Promise<{provider:string}>}){const {provider}=await params; const item=educationProviders.find(x=>x.slug===provider); if(!item) notFound(); return <div className="teksum-dashboard-page min-w-0 w-full flex flex-1 flex-col gap-6"><Link href="/dashboard/exam-pins" className="inline-flex w-fit items-center text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="mr-2 size-4"/>All education</Link><div><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-500">{item.name}</p><h1 className="mt-2 text-3xl font-black tracking-tight">Choose a product.</h1></div><div className="grid gap-3 sm:grid-cols-2">{item.products.map(([slug,title])=><Link key={slug} href={`/dashboard/exam-pins/${provider}/${slug}`} className="group rounded-2xl border bg-card p-5 transition hover:border-emerald-500/40"><div className="flex items-center justify-between"><div><p className="font-bold">{title}</p><p className="mt-1 text-sm text-muted-foreground">Current price and quantity options will be shown before you confirm.</p></div><ArrowRight className="size-5 text-emerald-500 transition group-hover:translate-x-1"/></div></Link>)}</div></div>}
