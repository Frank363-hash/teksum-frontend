"use client"
import { useEffect, useState } from "react"
import { ShieldCheck, Database, Server, LockKeyhole } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { apiFetch } from "@/lib/teksum-api"
export default function Page(){const [profile,setProfile]=useState<any>(null);useEffect(()=>{apiFetch<any>("/me").then(setProfile).catch(()=>{})},[]);return <div className="teksum-admin-page min-w-0 w-full"><h1 className="text-3xl font-black">System Settings</h1><p className="mt-1 text-sm text-muted-foreground">Operational configuration visibility for administrators.</p><div className="mt-6 grid gap-4 md:grid-cols-2"><Card className="rounded-2xl"><CardHeader><CardTitle className="text-base"><ShieldCheck className="mr-2 inline size-4 text-emerald-500"/>Access control</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><div className="flex min-w-0 items-start justify-between gap-4"><span className="text-muted-foreground">Current role</span><b className="min-w-0 break-words text-right">{profile?.role||'—'}</b></div><div className="flex min-w-0 items-start justify-between gap-4"><span className="text-muted-foreground">MFA</span><b className="min-w-0 break-words text-right">{profile?.mfaEnabled?'Enabled':'Required for admin access'}</b></div></CardContent></Card><Card className="rounded-2xl"><CardHeader><CardTitle className="text-base"><Server className="mr-2 inline size-4 text-emerald-500"/>Service infrastructure</CardTitle></CardHeader><CardContent className="space-y-3 text-sm text-muted-foreground"><p><Database className="mr-2 inline size-4"/>Catalogue and transaction state are controlled by the TEKSUM backend.</p><p><LockKeyhole className="mr-2 inline size-4"/>The admin secret is injected by the Next.js server proxy and is never exposed to browser JavaScript.</p></CardContent></Card></div></div>}
